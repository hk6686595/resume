# 崔伊超 · 面试问答准备

> 面向岗位：Windows 客户端 / C++（C#）/ 互动展项 / 全栈工程师
> 主线人设：**近 10 年客户端 + 软硬件联动经验，能独立打通「上位机 → 硬件 → 服务端 → 前端」全链路，熟练用 AI 工具提效。**

---

## 一、开场与自我介绍

### Q1. 请做个自我介绍
**回答框架（60~90 秒）**

- 我叫崔伊超，近 10 年开发经验，长期在合肥磐石科技做科技馆互动展项和工具软件研发。
- 技术主线是 **C/C++ 和 C#**：C++ 用 openFrameworks 做实时互动视觉，C# 用 WPF/.NET 做 Windows 客户端和工具；
  也用 **Unity** 做过大型沉浸式展项（花海、时钟墙等）。
- **软硬件联动**是我的强项：串口、DMX512、雷达、传感器、投影映射都做过，能把上位机程序控制到具体硬件。
- 工具链方面，我做过雷达标定、摄像头标定、项目授权系统，也长期开发维护公司内部的 **Unity 通用框架**，
  帮团队统一规范和快速交付。
- 近几年个人项目往服务端和全栈延伸：用 **Node.js + WebSocket + SQLite** 做了在线象棋平台和
  CommuSystem 局域网设备监控系统，客户端是 WPF，前后端协议自己设计。
- 日常熟练使用 Cursor、OpenCode CLI、Pi CLI 等 AI 工具，常用 DeepSeek、Grok、GPT、Gemini 辅助开发，
  明显提升了交付效率。

> **金句**：我是一个「能把软件和硬件接起来、也能把客户端和服务端接起来」的工程师。

### Q2. 为什么想离职 / 换工作？
建议方向：现有展项项目交付节奏稳定，希望进入更聚焦软件产品/客户端研发的团队，
把多年积累的实时渲染、性能优化、跨端通信能力用得更深，同时系统性地做工程化和架构。

### Q3. 你的职业规划？
短期深耕 Windows 客户端 + 实时互动方向，补齐 Electron / 现代前端与云侧能力；
中期成长为能独立负责一条产品线架构和交付的技术负责人。

---

## 二、C / C++ 基础与现代 C++

### Q4. C++ 内存管理你怎么理解？
- 栈/堆/静态区/常量区；`new/delete` 与 `malloc/free` 的区别（构造析构、类型安全、失败行为）。
- **RAII** 是核心：资源获取即初始化，用对象生命周期管理内存、文件、锁、socket。
- 优先用智能指针：`unique_ptr`（独占，默认首选）、`shared_ptr`（共享，注意引用计数与循环引用）、`weak_ptr`（打破循环）。
- C++20 里继续减少裸指针和裸 new；容器优先 `std::vector`、`std::string`。

### Q5. 讲讲你对 C++11/14/17/20 的掌握
- **C++11**：`auto`、`nullptr`、范围 for、lambda、右值引用与移动语义、`=default/=delete`、`std::thread`、智能指针。
- **C++14/17**：泛型 lambda、`std::optional`、`std::variant`、结构化绑定、`if constexpr`、`std::filesystem`。
- **C++20**：concepts、ranges、`std::format`、协程、三路比较 `<=>`、span。项目里用过 `std::format` 简化日志/字符串拼接。
- 一句话：**现代 C++ 更强调值语义、编译期检查和零开销抽象。**

### Q6. 移动语义和完美转发是什么？
- 移动语义：通过右值引用 `T&&` 把资源「偷」过来，避免深拷贝；配合 `std::move` 把左值转成右值。
- 完美转发：模板里用 `T&&` 万能引用 + `std::forward<T>` 保持参数的左/右值属性转发给下层。
- 典型场景：容器扩容、工厂函数返回大对象、`emplace_back` 就地构造。

### Q7. 多线程与并发你怎么用？
- `std::thread` / `std::mutex` / `condition_variable` / `atomic` / `future`。
- 互动展项里常见模式：**主线程只做渲染，通信/采集/解码放到工作线程**，通过线程安全队列或双缓冲把数据交给渲染线程。
- 注意点：锁粒度尽量小、避免在持锁时做耗时/回调、警惕死锁（固定加锁顺序或用 `scoped_lock` 一次锁多个）。
- 无锁可选：原子变量 + 环形缓冲，适合高频音频/传感器数据。
- 比线程更好维护的替代：`std::async` / 线程池，控制线程数量。

### Q8. 如何排查内存泄漏和性能问题？
- 工具：Visual Studio 诊断工具 / CRT 调试堆（`_CrtDumpMemoryLeaks`）、AddressSanitizer、性能分析器（采样）。
- 思路：先定位是泄漏还是高频分配，再看热点函数；渲染性能看帧耗时分解（逻辑/渲染/上传/同步）。
- 常见优化：对象池、避免每帧分配、合批渲染、纹理压缩、降低不必要的数据拷贝（移动/引用）、限制帧率。

---

## 三、C# / .NET / WPF

### Q9. WPF 的 MVVM 你怎么理解？
- Model-View-ViewModel：View 只负责展示（XAML + Binding），ViewModel 持有状态和命令，Model 是数据和业务。
- 核心机制：**依赖属性（DependencyProperty）**、**数据绑定（Binding）**、**命令（ICommand）**、
  **INotifyPropertyChanged / ObservableCollection**、DataTemplate。
- 好处：UI 与逻辑解耦、可测试、多人协作（UI 和逻辑分离）。
- 项目实践：象棋客户端棋盘用自定义控件/Canvas 渲染，对局状态用 ViewModel 绑定，
  走子命令走 ICommand，服务端推送通过 Dispatcher 切回 UI 线程更新。

### Q10. 跨线程更新 UI 怎么处理？
- WPF 控件有线程亲和性，工作线程不能直接改 UI。
- 用 `Dispatcher.Invoke/BeginInvoke`（或 `SynchronizationContext`）切回 UI 线程。
- 更优雅：ViewModel 里用 `async/await` 自动回到 UI 上下文；后台任务 `Task.Run` 处理耗时逻辑。
- 注意：避免 UI 线程阻塞，所有网络/文件/串口 IO 都异步化。

### Q11. async/await 原理？
- 编译器把方法改写为状态机；遇到未完成的 await 就注册续体并返回，完成后再继续。
- 关键点：`async` 不等于自动开线程，IO 型操作本来就是异步的；
  CPU 密集用 `Task.Run` 移到线程池。防止 `async void`（除事件处理器），避免 `.Result/.Wait()` 死锁。

### Q12. C# 值类型与引用类型、GC？
- 值类型（struct）存在栈/内联在对象里，赋值拷贝；引用类型在托管堆，赋值是引用拷贝。
- GC 分代：Gen0/1/2 + LOH，标记-清除-压缩；关注频繁分配、大对象、`IDisposable` 非托管资源。
- 优化：`Span<T>`/`Memory<T>`、`ArrayPool`、结构体避免装箱、`StringBuilder`。

### Q13. 委托、事件、闭包？
- 委托是类型安全的函数指针；`event` 是对委托的封装（外部只能 +=/-=）。
- 闭包会捕获外部变量并延长其生命周期，注意循环里捕获变量导致的问题（用局部副本）。
- 事件要记得取消订阅，否则可能内存泄漏。

---

## 四、Unity / openFrameworks / 实时互动视觉

### Q14. Unity 里你做过什么？内部框架做了什么？
- 做过沉浸式花海、时钟墙等展项：粒子系统、动作/雷达交互、多屏画面融合。
- 内部 Unity 框架目标：**统一场景管理、硬件通信封装、通用组件、调试工具**，
  让新展项能复用通信层与常见交互逻辑，缩短交付周期。
- 框架分层：通信层（串口/网络/DMX）→ 数据层（配置、状态）→ 表现层（组件、特效）→ 工具层（调试面板、标定）。

### Q15. Unity 性能优化怎么做？
- 渲染：合批（Static/Dynamic Batching、GPU Instancing）、减少 Draw Call、图集、LOD、遮挡剔除。
- CPU：减少 `Update` 里的分配和 `GetComponent`（缓存引用）、避免频繁 GC、对象池。
- 粒子：控制粒子数量/分辨率，使用 GPU 粒子；粒子数量是沉浸式展项的常见瓶颈。
- 多屏：注意每屏分辨率和总像素带来的填充率压力。
- 工具：Profiler（CPU/GPU/Memory）、Frame Debugger。

### Q16. openFrameworks 相比 Unity 的优势？
- 纯 C++ 开源框架，控制力强、依赖轻、启动快，适合实时音频分析、底层图形/硬件对接和定制渲染。
- 律动光柱项目用它做**实时音频分析与节奏检测**，直接驱动光柱矩阵，延迟更低、可控性更好。
- 代价：没有 Unity 的可视化编辑器和现成生态，需要自己搭工具链——这也是我做工具和框架的原因。

### Q17. 说说投影映射 / 多屏融合的原理
- 投影映射：把投影仪画面通过透视变换/单应性矩阵（Homography）映射到不规则载体，
  需要做几何校正 + 边缘融合 + 遮罩。
- 摄像头标定工具负责求内参/外参，计算映射关系并生成校正参数，现场用遮罩工具做遮蔽与融合调试。
- 关键：坐标变换一致性（相机坐标系 ↔ 屏幕坐标系 ↔ 投影坐标系）。

---

## 五、OpenCV / 图像与视觉

### Q18. OpenCV 你常用哪些能力？
- 人脸滤镜：`CascadeClassifier`/DNN 做人脸检测，再叠加滤镜；
- 摄像头标定：棋盘格角点检测 `findChessboardCorners` → `calibrateCamera` 求内外参 →
  `undistort` / `initUndistortRectifyMap` 校正，投影映射用 `findHomography` + `warpPerspective`。
- 常用：色彩空间转换、滤波、阈值、形态学、轮廓、特征点匹配、视频读写。

### Q19. 图像处理性能怎么优化？
- 降分辨率/ROI 只处理感兴趣区域；用 `UMat`/OpenCL 或 GPU；
- 减少重复的格式转换和内存拷贝；
- 算法层面：不需要精度的地方用整数/查表，跳帧处理。

---

## 六、网络与协议

### Q20. TCP 和 UDP 的区别，什么时候用哪个？
- TCP：面向连接、可靠、有序、有流控/拥塞控制，开销大；适合文件、命令、需要可靠性的控制通道。
- UDP：无连接、不可靠、低延迟、开销小；适合实时性优先、可容忍丢包的场景（实时传感器/视频帧）。
- 实践：**控制命令走 TCP/WebSocket，实时屏幕流和传感器高频数据可考虑 UDP 或帧丢弃策略**。

### Q21. 三次握手、四次挥手讲一下
- 三次握手：SYN → SYN+ACK → ACK，同步双方初始序列号并确认双向可达。
- 四次挥手：FIN → ACK → FIN → ACK，因为 TCP 半关闭，两个方向要分别关闭；主动方最后进入 TIME_WAIT。

### Q22. HTTP 和 WebSocket 的区别？
- HTTP 是请求-响应、无状态、短连接（HTTP/1.1 可 keep-alive），服务端无法主动推送。
- WebSocket 通过 HTTP Upgrade 握手后升级为全双工长连接，服务端可主动推，适合实时通信。
- 什么时候用：**需要服务端主动推送、低延迟、双向交互**（在线对战、设备监控、终端输出）选 WebSocket；
  普通 CRUD/文件传输用 HTTP 更简单，还能利用缓存、断点续传、成熟鉴权。

### Q23. Protobuf 了解吗？和 JSON 比有什么优劣？
- Protobuf 是二进制、基于 `.proto` schema 的序列化格式，需要预先定义消息结构并生成代码。
- 优点：体积小、序列化/解析快、跨语言、schema 显式且有版本兼容规则（字段编号）。
- 缺点：可读性差、需要编译生成代码、schema 变更要走流程。
- 选择：**内部高性能/跨语言链路用 Protobuf，调试友好、对前端/浏览器友好的场景用 JSON。**
- 我在 CommuSystem 里协议按语言无关设计，控制帧用 JSON 便于调试，二进制数据帧用自定义帧头承载大块数据。

### Q24. WebSocket 怎么做鉴权、心跳、断线重连？
- 鉴权：浏览器无法在握手时带自定义头，所以用**首消息鉴权**——连接后 10 秒内发 `auth.login` 携带 JWT/令牌，超时断开。
- 心跳：客户端定时发 ping，服务端用最后心跳时间做**超时离线检测**（如 30s 无心跳判离线）。
- 断线重连：客户端指数退避重连；服务端维护连接状态，重连后重新注册设备/重放订阅。
- 生产注意：反向代理要开 WebSocket 升级、注意空闲超时、单机连接数与消息限流。

### Q24b. MQTT 协议了解吗？它和 WebSocket 有什么区别？
- MQTT 是轻量级的**发布/订阅**消息协议，基于 TCP（也有 MQTT over WebSocket），非常适合低带宽、
  不稳定网络下的**物联网设备通信**；核心概念是 Broker、Topic、QoS、Retain、遗嘱消息（LWT）。
- 与 WebSocket 的区别：WebSocket 是**传输层长连接通道**，只管实时双向通信，消息模型要自己设计；
  MQTT 是**应用层消息协议**，自带发布/订阅路由和 QoS 等级（0 至多一次 / 1 至少一次 / 2 恰好一次）。
- 设备上报传感器数据、状态心跳这类「多设备、一对多分发」场景，MQTT 比自建 WebSocket 更省事；
  而需要低延迟双向交互、终端会话的场景，WebSocket 更直接。
- 安全与可靠性：用户名密码 / 双向 TLS、QoS 选择、保留消息与遗嘱消息（设备异常断开时发出通知）。
- 实践联系：展项里的传感器/设备状态上报可用 MQTT 解耦，管理端订阅 Topic 实时刷新，
  与传统串口/DMX 控制形成「本地实时控制 + 网络状态汇聚」的分工。

---

## 七、硬件联动（你的差异化优势）

### Q25. 串口通信要注意什么？
- 参数匹配：波特率、数据位、停止位、校验位必须和硬件一致。
- 粘包/拆包：串口是字节流，要按**协议帧头+长度+校验**来分包，不能假设一次读到一帧。
- 线程：读用独立线程 + 缓冲队列，避免阻塞 UI；写加锁保证帧完整。
- 异常：拔插设备、端口占用、超时重试；做自动重连。
- 我在律动光柱、磁片墙、磁液贪食蛇里都做过串口/DMX 硬件控制。

### Q26. DMX512 是什么？
- 舞台/灯光常用协议，基于 RS-485，一帧 512 个通道，每通道 0~255，靠起始码 + 刷新率（约 44Hz）传输。
- 上位机把音频/交互数据映射到通道值，通过 USB-DMX 或串口发出，驱动灯具/光柱。

### Q27. 雷达/传感器交互怎么做的？
- 雷达输出目标坐标（距离+角度），上位机做坐标变换到屏幕坐标，再做平滑/滤波（卡尔曼或简单滑动平均）避免抖动跳变。
- 多目标时做跟踪与 ID 匹配；为避免遮挡和误触发，会设交互区域和去抖阈值。
- 现场最大痛点之一是坐标标定，所以我做了**雷达标定工具**：可视化实时点位，一键求解映射参数。

### Q28. 展项长期运行稳定性的保障（现场交付经验）
- **看门狗**：监控主循环心跳，卡死自动重启程序。
- **异常隔离**：硬件通信/网络异常不能拖垮渲染主线程；关键操作 try-catch + 降级到默认动画。
- **配置化**：分辨率、设备参数、映射参数走配置文件，现场可改不用重新编译。
- **日志**：关键路径写本地日志，便于现场远程排查。
- **开机自启 + 长时间压测**：交付前跑 72 小时以上的稳定性测试。

---

## 八、服务端 / Node.js / 全栈

### Q29. 你个人项目的技术架构讲一下（CommuSystem）
- 目标：内网小规模场景的**设备监控 + 远程运维**。
- 三层：
  - **Vue 3 管理端**（Vite + TS + Pinia + Element Plus + ECharts + xterm.js）；
  - **Node.js 中枢**（Express + ws + better-sqlite3）：统一鉴权、路由、持久化、审计；
  - **C# / .NET 8 WPF Agent**（托盘应用）：采集指标、执行命令、终端、屏幕流、文件。
- 双 WebSocket 通道：`/ws/agent`（设备）和 `/ws/admin`（管理员）。
- 功能：设备看板、CPU/内存历史、一次性命令、ConPTY 交互式终端、只读实时屏幕（JPEG 1–5fps）、
  中转文件收发（sha256 校验 + 断点续传）、全量审计。
- 安全：管理员 JWT、设备注册密钥 + 设备令牌、本地与服务端双重能力开关、限流、可选 TLS。
- 测试：Node 端 `node:test` 单测 + 协议集成测试，前端 vitest，C# 端 xunit；还写了跨语言冒烟工具验证链路。

### Q30. 为什么要用 WebSocket 而不是轮询？
- 设备状态、终端输出、屏幕帧都是**高频、服务端主动推**的数据，轮询延迟高、浪费带宽。
- WebSocket 全双工，一次握手长期复用，适合实时推送和双向命令。

### Q31. 交互式终端怎么实现的（ConPTY）？
- Windows 上用 **ConPTY（Pseudo Console）** 创建伪终端进程（PowerShell/cmd），
  把它的 stdin/stdout 通过管道接到 Agent，再经 WebSocket 转发到浏览器 **xterm.js**。
- 好处：完整支持交互式程序（颜色、光标、清屏等 ANSI 序列），不是简单的一次性命令。
- 难点：字节流分帧、窗口尺寸同步（resize）、进程生命周期与断开清理、审计落盘。

### Q32. 实时屏幕流怎么设计？为什么只读？
- Agent 定时截屏 → 编码 JPEG（可调分辨率/质量）→ 二进制帧经 WebSocket 推送 → 前端渲染。
- 只读是**安全设计**：不做鼠标键盘远控，只用于监看，避免被滥用成远控木马。
- 优化：限制帧率、按需订阅、变化检测跳帧、限制带宽优先级。

### Q33. 文件传输的可靠性怎么保证？
- HTTP 传输 + **sha256 校验**确保内容完整；**断点续传**用分片 + 偏移量记录。
- 大文件不占用 WebSocket 控制通道，避免阻塞实时消息。
- 服务端做大小限制、类型白名单、落盘目录隔离。

### Q34. 在线象棋平台的实时同步怎么保证一致性？
- 服务端作为权威：所有走子先在服务端校验（棋规、轮次、胜负），通过后才广播给双方。
- 客户端本地即时反馈 + 服务端确认回滚（乐观更新）。
- 断线重连后拉取完整棋局状态全量同步。
- 用房间/对局 ID 隔离，消息带序号防乱序。
- 接入 eleeye 引擎做本地人机对战；cpolar 内网穿透让外部玩家也能连。

### Q35. Node.js 的单线程模型和适用场景？
- Node 是单线程事件循环 + 非阻塞 IO，适合 IO 密集型（网关、实时通信、API）。
- CPU 密集任务会阻塞事件循环，可以用 `worker_threads`、子进程或拆到 C# 服务。
- 我项目里截屏编码这种 CPU 活放在 Agent（C#）侧，Node 只做中转和持久化。

### Q36. better-sqlite3 / SQLite 的选择理由与注意事项？
- 内网小规模、单机部署，SQLite 零运维、单文件、事务可靠，配合 WAL 模式读写性能足够。
- 注意：写入串行化、避免长事务、合理建索引；数据量大或需要并发多写时再考虑 Postgres。
- 指标数据用「原始采样 + 整点聚合」两级存储控制增长，按 retention 清理。

---

## 九、项目深挖（STAR 法则准备）

准备好每个标杆项目的 **背景 / 目标 / 你的职责 / 难点 / 结果**，尤其是「难点」和「你的决策」。

| 项目 | 一句话卖点 | 预判追问 |
| --- | --- | --- |
| 律动光柱 | 实时音频分析驱动光柱矩阵，音画同步 | 音频节拍怎么检测？延迟怎么控？多线程怎么分工？ |
| 沉浸式花海 | 大型沉浸式粒子互动 + 雷达交互 + 多屏融合 | 粒子性能怎么优化？雷达抖动怎么处理？ |
| 磁片墙 | 图像驱动大量磁片翻转成像 | 大量设备的控制协议？指令顺序和时序？ |
| 雷达标定工具 | 现场标定提效 | 坐标变换矩阵怎么求？怎么做到可视化？ |
| 内部 Unity 框架 | 沉淀通信/组件/调试能力，多项目复用 | 框架怎么抽象？怎么保证不侵入业务？ |
| 项目授权系统 | 按项目/时间/设备授权 | 加密方案？防篡改？离线授权怎么做？ |
| CommuSystem | 全栈设备监控，跨语言协议 | 协议设计、鉴权、终端、屏幕流（见上） |
| 在线象棋 | Node + WPF 全链路实时对战 | 一致性、断线重连、AI 引擎接入 |

### Q37. 讲一个你解决过的最难的问题
**模板**：现象 → 定位过程 → 根因 → 方案与取舍 → 结果沉淀。
建议选一个真实例子，比如：
- 现场投影映射对不齐 → 做摄像头标定工具，从手工调到可视化自动求解；
- 展项跑几小时后卡顿 → 定位到每帧分配/粒子过多 → 对象池 + 降粒子 + 分级渲染；
- 串口粘包导致动作错乱 → 设计带长度和校验的帧协议。

### Q38. 讲一次和硬件/现场团队协作解决冲突的经历
体现沟通：先复现、拿数据、明确责任边界、给临时方案保证开展、再根治。
例如硬件响应延迟导致动画不同步，最终通过「软侧预测 + 硬件回执校准」解决。

---

## 十、工程能力与协作

### Q39. 代码质量怎么保证？
- 分层与职责单一；接口稳定、命名清晰；
- 关键模块写单测（尤其协议解析、状态机、坐标变换）；
- Code Review、统一规范；日志分级；异常不吞。
- 我在 CommuSystem 里对协议层和身份逻辑都加了测试，避免跨语言实现不一致。

### Q40. 用过哪些开发工具？
Visual Studio、Git、Cursor / OpenCode CLI / Pi CLI（AI 编程）、性能分析器、
串口/网络调试助手（部分是我自己写的）、OpenCV、Unity Editor。

### Q41. 你怎么用 AI 工具提升效率？
- 场景：快速生成样板代码、补测试、重构建议、读陌生代码/文档、写文档与脚本。
- 原则：**AI 产出必须自己审查和验证**，尤其协议、并发、安全相关代码；
  用清晰的上下文和约束描述需求，让 AI 做「初稿」而不是「最终决策」。
- 代表成果：个人项目和一批 Python 工具（串口助手、网络助手、图片处理、PNG 转 WebM、投影遮罩、人脸滤镜）
  都借助 AI 提升了开发速度。

### Q42. 你和产品/美术/硬件同事怎么协作？
- 需求阶段明确交互与硬件边界和验收标准；
- 提供可调参数和调试工具给美术/现场，减少来回改代码；
- 主动暴露风险（硬件不确定项、性能上限）。

---

## 十一、常见的「弱点」与应对

### Q43. 你觉得自己有什么不足？
- 坦承：过去偏客户端/项目制，云原生、大规模分布式和高并发经验相对少，
  所以主动用 Node.js 做全栈项目补这块；前端工程化和 Electron 也在系统学习中。
- 优势是底层、实时、软硬件结合的深度，这些可迁移性强。

### Q44. 为什么觉得自己能胜任这个岗位？
用「能力映射」回答：把 JD 的每条要求和自己的项目证据一一对应（客户端经验 → WPF/工具；
实时 → openFrameworks/Unity；协议 → TCP/UDP/WebSocket/Protobuf；全栈 → 个人项目）。
最后强调稳定交付和现场落地能力。

---

## 十二、反向提问（面试结尾准备 3 个）

1. 团队目前最想解决的技术问题是什么？这个岗位前 3–6 个月的核心目标是什么？
2. 团队的技术栈和协作流程（代码评审、测试、发布节奏）是怎样的？
3. 这个岗位是偏长期产品迭代，还是偏项目交付？对客户端/实时互动经验的需求有多重？

---

## 附：技术速查清单（临阵复习）

- **C++**：RAII、智能指针、移动语义、完美转发、多线程与锁、C++20 concepts/ranges/format。
- **C#**：值/引用类型、GC 分代、async/await、委托事件、LINQ、IDisposable、Span。
- **WPF**：MVVM、依赖属性、Binding、ICommand、Dispatcher、DataTemplate、性能优化。
- **Unity**：生命周期、协程、粒子、Profiler、合批、对象池、多屏渲染。
- **OpenCV**：色彩/滤波/阈值/形态学/轮廓、标定、单应性、warpPerspective。
- **网络**：TCP/UDP、三次握手四次挥手、HTTP/WS 区别、心跳重连、Protobuf vs JSON、MQTT 发布订阅与 QoS。
- **硬件**：串口参数与粘包、DMX512、雷达坐标变换与滤波、看门狗与稳定性。
- **服务端**：Node 事件循环、WebSocket 双通道、JWT、SQLite/WAL、审计与限流、断点续传。
- **AI**：Cursor / OpenCode CLI / Pi CLI、DeepSeek / Grok / GPT / Gemini、AI 产出必须审查。

> 每条回答尽量遵循：**结论先行 → 原理/机制 → 我的实践 → 取舍与结果**。
